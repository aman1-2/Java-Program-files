public class typeCasting_10{
    public static void main(String args[]){
        /*Type casting  is the forcefull method to convert the it form one data typt to 
        another data type.

        That's why it is know as Narrowing data type.

        because we call this only when we have to convert a smaller data type into larger data
        type as it doesn't do this on it own.*/

        double num=12.06;
        int a=(int)(num);

        System.out.println("Using the value of the variable a="+a);
    }
}